# Docker profile

spring:
  config:
    activate:
      on-profile: docker

  kafka:
    bootstrap-servers: kafka:9092

  redis:
    host: redis
    port: 6379

logging:
  level:
    root: INFO
    io.conflictradar: INFO